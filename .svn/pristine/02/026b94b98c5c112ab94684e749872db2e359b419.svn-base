import GV from '../GV'
import isTouch from '../util/isTouch'
import { control } from 'leaflet'

GV.Buttons.zoom = function (btnOptions, map) {
  'use strict'
  if (isTouch()) {
    return null
  }

  return control.zoom(btnOptions)
}
