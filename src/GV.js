import globals from './globals';
import init from './init';
import * as config from './config';
import util from './util';

var map = null;
var Buttons = [];

export default {
    init,
    config,
    globals,
    util,
    map,
    Buttons
};

